const navBarStyle = {
  backgroundColor: 'Gainsboro',
  height: '50px',
  display: 'flex',
  alignItems: 'center',
  borderRadius: '0px',
};

const cartAndProfileStyle = {
  position: 'absolute',
  right: '1%',
};

const style = {
  navBarStyle,
  cartAndProfileStyle,
};

export default style;
